import React, { FC } from 'react';
import { IProjects } from 'types/projectTypes';

interface Props {
  project: IProjects;
  state: object;
}

const SearchProjectsItem: FC = () => {
  return <div>SEARCHPROJECTSITEM</div>;
};

export default SearchProjectsItem;
